---
import MainPage from "../../components/MainPage.astro";

export function getStaticPaths() {
  return [
    { params: { lang: "en" } },
  ];
}

const { lang } = Astro.params as { lang: "en" };
---
<MainPage lang={lang} />
