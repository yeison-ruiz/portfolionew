---
import MainPage from "../../components/MainPage.astro";

export function getStaticPaths() {
  return [
    { params: { lang: "es" } },
  ];
}

const { lang } = Astro.params as { lang: "es" };
---
<MainPage lang={lang} />
